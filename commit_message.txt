feat: motor de descompilación JADX Core, auditoría criptográfica con BouncyCastle, desofuscación ProGuard ReTrace y descompresión multi-algoritmo

- Integrar servicio de descompilación Java de alto nivel con JADX Core (JadxDecompilerService):
  * Adición de io.github.skylot:jadx-core para descompilar bytecode Dalvik (.dex) a código fuente Java real y estructurado directamente en el teléfono móvil.
  * Configuración de JadxArgs con desofuscación heurística, saneamiento defensivo de identificadores no válidos y resolución transversal de dependencias de clases.
  * Conmutador en FileDetailScreen para alternar dinámicamente entre desensamblado Smali (baksmali) y código Java recompilado por JADX con resaltado y selector de clases.
  * Operaciones en segundo plano mediante ComputeDispatcher en ApkViewModel para evitar bloqueos del hilo principal de UI.

- Implementar suite criptográfica y análisis de certificados con BouncyCastle (BouncyCastleCryptoService):
  * Adición de org.bouncycastle:bcprov-jdk18on y bcpkix-jdk18on para auditoría forense de firmas APK en el directorio META-INF/ (.RSA, .DSA, .EC, MANIFEST.MF, CERT.SF).
  * Extracción de certificados X.509, sujeto (Subject), emisor (Issuer), número de serie, algoritmo de firma y períodos de vigencia.
  * Cálculo de huellas digitales criptográficas (Fingerprints) en SHA-256, SHA-1 y MD5 con botones de copiado rápido al portapapeles.
  * Soporte de hashes avanzados (SHA3-256, Keccak, RIPEMD-160) y decodificación ASN.1 / PKCS#7 SignedData.

- Implementar servicio de desofuscación ProGuard ReTrace (ProguardRetraceService):
  * Integración de com.guardsquare:proguard-retrace para procesar archivos mapping.txt de ProGuard/R8.
  * Desofuscación en tiempo real de nombres de paquetes, clases, métodos y atributos en stack traces y código descompilado.
  * Exclusión de artefactos duplicados de proguard-base en Gradle para evitar colisiones de empaquetado.

- Implementar motor unificado de descompresión multi-algoritmo en streaming (DecompressionService):
  * Soporte integral para Zstandard (zstd-jni), LZ4 (lz4-java), Brotli (brotli-dec), Bzip2 (commons-compress), XZ/LZMA, GZIP y Deflate.
  * Detección automática de algoritmos de compresión mediante análisis de magic bytes y firmas binarias.
  * Descompresión directa en streaming hacia context.cacheDir con búfer seguro de 32 KB para prevenir problemas de memoria (OOM).

- Actualizar y refactorizar el visor/editor de datos binarios (BinaryDataParser y BinaryDataViewerContent):
  * Integración con DecompressionService para descompresión inmediata de flujos y archivos empaquetados .dat y .bin.
  * Integración con BouncyCastleCryptoService para análisis de integridad criptográfica y cálculo de hashes de bloques binarios.
  * Optimizaciones en la interfaz táctil: visualización en bloques de 16 bytes, cálculo de Entropía de Shannon (0.0 a 8.0) y extractor de cadenas legibles con filtrado reactivo.

- Actualizar repositorios, viewmodels y pantallas (ApkExtractorRepository, ApkViewModel, FileDetailScreen):
  * Incorporación de flujos reactivos StateFlow para descompilación JADX (jadxDecompiledCode, isJadxDecompiling).
  * Soporte de visualización de firmas de certificados APK en FileDetailScreen con tarjeta descriptiva de seguridad.
  * Manejo seguro de insets del sistema (statusBarsPadding y navigationBarsPadding) preservando compatibilidad completa con pantallas móviles.
